fasta.py is a module for fasta file loading.

pfind.py identifies palindromes.

palindrome_summary_to_excel summarizes palindrome data into a excel format.

Details about input format, parameters etc are listed inside the python files.
